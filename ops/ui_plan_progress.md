# UI visual-language ledger — plan: ops/ui_plan_data.json (phases 0-5)
Phase 0.1: ambient particle-field prototype built in .lavish/phase0-particle-spike.html -- sparse accent-green (#9fe870) dots on #0d0f0c, live sliders for count/speed/size/twinkle, a reduced-motion checkbox that freezes drift entirely rather than slowing it. Vanilla canvas, throwaway.
Phase 0.1: complete.
Phase 0.2: particle-ring loader prototype built in the same file -- dots evenly spaced on a circle, continuous rotation plus a per-dot sine-wave radius/size pulse, live sliders for dot count/radius/loop-duration/wave-depth, reduced-motion checkbox freezes to a static evenly-spaced ring.
Phase 0.2: complete.
Phase 0.3: intro-sequence prototype built in the same file -- classic particle-to-text morph (sample glyph pixels from an offscreen canvas rendering "tradefabe" at 900 weight, particles ease from random start points onto those targets), headline reveals word-by-word after a configurable delay, then the whole intro fades and a mock row-list backdrop (4 fake book rows) fades in underneath. Sliders for assembly/delay/hold/settle durations, a Replay button, reduced-motion checkbox skips straight to the settled end-state.
Phase 0.3: complete.
Phase 0.1-0.3: all three prototypes live in one tabbed artifact, dark theme tokens pulled directly from frontend/tailwind.config.js and frontend/src/index.css (bg #0d0f0c, surface #181c15, accent #9fe870, ink #f2f5ef, ink-muted #7d8877, Space Grotesk + IBM Plex Mono).
Phase 0.4: live review started via lavish-axi poll. Dave's first report: ring loader and intro sequence canvases not rendering. Root cause: those two canvases live inside tab panels that start `display:none`, so `getBoundingClientRect()` returned 0x0 at page load (only the default-visible "Ambient field" tab measured correctly). Fixed by exposing `window.__activateRing`/`window.__activateIntro` from each prototype's IIFE and calling them (re-measure canvas + restart animation) from the tab-click handler once the panel is actually visible. Sent back via --agent-reply, awaiting reload/re-review.
Phase 0.4: second report -- ring loader confirmed working after the tab-activation fix, but intro sequence still not loading, plus ring/ambient field called "too basic," visual quality needs to be settled now rather than deferred. Found the real intro bug: `sampleTextTargets()` indexed `getImageData` using the float CSS `w`/`h` as the pixel-array row stride instead of the offscreen canvas's actual integer width/height -- this drifts the row stride past row 0 and scrambles nearly every alpha lookup, so almost no particle targets were ever sampled. Fixed by flooring to integer `ow`/`oh` and using those consistently for canvas size, font-fit, and indexing.
Phase 0.4: installed `iart-ai/webgl-animation-skills@particle-system` as a project skill (`.agents/skills/particle-system`, `npx skills add`, same convention as the existing `ibelick/ui-skills` skills) and used its recipes to substantively upgrade all three prototypes rather than just polish existing code: (1) ambient field switched from constant-velocity linear drift to per-particle wander force + drag (semi-implicit Euler), added depth-based parallax (near/mid/far size+speed), added a spatial-grid constellation network (connectDots(), O(n)) connecting nearby particles with faint fading lines, and switched dot rendering to soft radial-gradient glow under `globalCompositeOperation='lighter'` for real additive brightening. (2) Ring loader dots now spring toward their rotating ring target via a damped spring force (stiffness/damping constants, semi-implicit Euler) instead of snapping directly to trig position each frame, giving real elastic lag on parameter changes; neighboring dots are connected into a glowing outline (also under 'lighter') instead of floating independently. (3) Intro assembly switched from a plain easeOutCubic glide to easeOutBack (slight overshoot-and-settle), and while still converging (assembleT < 1) draws the same constellation network between in-flight particles so the wordmark reads as a network resolving into place, not a fading point cloud. Sent back via --agent-reply, awaiting reload/re-review.
Phase 0.4: Dave's verdict -- "the current settings i found i like the most for all 1 2 and 3." Locked in as validated_params in ops/ui_plan_data.json phase 0: ambient field (count 211, drift speed 0.046, dot size 1.2, twinkle 0.15), ring loader (dot count 64, radius 76px, loop duration 2700ms, wave depth 9px), intro (assembly 1200ms, headline delay 1000ms, hold 1100ms, settle fade 600ms). Techniques to carry into Phase 1's real components are also recorded there (wander-force drag, constellation network, additive glow, spring-toward-target ring assembly, easeOutBack, integer-pixel getImageData indexing).
Phase 0.4: complete. Session ended via lavish-axi.
Phase 0.5: complete -- spike file lives at .lavish/phase0-particle-spike.html, already covered by the repo's .gitignore (.lavish/ entry), never committed, never staged. Nothing further to discard; validated_params above is what carries forward.
Phase 0: ALL STEPS COMPLETE. Per Dave's explicit instruction ("build only the first step"), stopping here -- Phase 1 (shell & foundation) not started.
Built ops/ui_plan.py -- AXI-standard CLI over ops/ui_plan_data.json (TOON output, --json escape hatch, content-first no-args view, list/show/next/step show/step start/step done, idempotent mutations, structured stdout errors, --help per command, --version fast path). Verified against real data: home/list/show/next/step-show all render correctly, unknown-flag/unknown-command/bad-id errors exit with the right codes, step start/done are idempotent and auto-append to this ledger on real state changes. Test run of `step start 1.1` / `step done 0.1` reverted afterward -- no real work started yet.
Phase 1.1: started -- ParticleField component + tests
Phase 1.1: complete -- ParticleField component + tests
Phase 1.2: complete -- Deeper sound design
Phase 1.3: complete -- Intro/reveal sequence
